const name = "Vikrant" //cannot modified

let rollNo = 60; // can be modified created to eliminate the scope error caused by var

var address = "Thane" //deprecated due to the scope error

gender = "male" // can be defined without explicite variable declaration

let age; // unknown undefined value

console.log(name) // used to print the value stored inside the variable, prints value of only single variable

console.table([name, rollNo, address, gender, age]) // print values of all variables mentioned